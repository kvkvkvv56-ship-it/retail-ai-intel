# 行业与竞对 AI 应用动态洞察助手 · 交付说明

| | |
|---|---|
| **线上站点** | <https://ai.kvkvkvv.site> |
| **代码仓库** | <https://github.com/kvkvkvv56-ship-it/retail-ai-intel> |
| **完整文档** | <https://ai.kvkvkvv.site/docs>（使用说明 / 作品文档 / 方法论 / 运行说明 / 技术方案） |
| **服务对象** | 京东零售 |
| **数据口径** | 知识库总量、观察范围等统计随流水线自动刷新；②里的漏斗与样例事件是某一轮的定格，用于展示链路 |

本文只覆盖交付要求的三项。更细的设计依据、踩坑记录与完整技术方案见站内文档。

---

# ① 交付物形态

一套**可运行、可复用的自动化情报流水线 + 只读查询站点**，不是提示词组合。

```
GitHub Actions（无人值守：事件库每天两轮 08:30 / 20:30，周报每周一份）
  └─ Python 流水线 S0–S8（仅标准库，无第三方依赖）
       ├─ data/*.jsonl              真相源，git 提交，行级 diff 可读
       │    └─ intel.db             SQLite 索引，五秒可从 JSONL 重建
       └─ web/public/api/v1/*.json  API 快照，同一次提交回仓库
                    ├─ raw.githubusercontent  Worker 优先读（数据更新无需重新部署）
                    └─ Cloudflare ASSETS      读不到时兜底
                                 └─ ai.kvkvkvv.site
```

**八个阶段**

| 阶段 | 做什么 | 谁来判 |
|---|---|---|
| S0 采集 | 六通道（rss / rsshub / jina / aihot / direct / exa） | — |
| S1 去重 | URL 精确 → SimHash → 标题归一化，加时间窗口与零成本关键词粗筛 | 规则 |
| S2 预筛 | 判是否与观察范围相关（便宜档模型，吃最大调用量） | 模型 |
| S3 抽取 | 结构化成事件草稿，提示词按信源一手性分流 | 模型 |
| S4 归并 | 同一件事的多篇报道合成一个事件 | 模型 |
| S4b 全库复核 | 向量召回高相似事件对 → 模型逐对裁决；同一件事只留最早进库的那条，后续报道并入作佐证 | 向量 + 模型 |
| S5 核验 | 置信度 / 落地阶段 / 新增旧闻，**全库重算** | 纯规则 |
| S6 关系边 | 同主体事件按时间连边 | 规则 |
| S7 周报 | 跨事件合成洞察，**每周一份**（见 3.3） | 模型 |

**分工的边界画在「能不能用确定性函数算出来」**：模型只负责语言理解（这条信息说了什么、属于哪一类），可信度、落地阶段、新增还是旧闻全部由规则裁决——输入相同必然输出相同，可回归、可审计。

**复用方式**：换一个行业只改两个 JSON（`config/<dataset>.json` 定义观察主体、领域、检索矩阵、关键词、窗口；`config/sources.<dataset>.json` 定义信源注册表与通道），然后 `python3 -m pipeline.run --dataset <名字>`。数据落 `data/<dataset>/`，与主数据集完全隔离。

> 已实测一次：新能源汽车与智能驾驶数据集，流水线代码一行未改，一轮跑出 45 个事件、526 条断言、1 期周报，成本 Exa $0.154 + LLM ¥0.59。该数据集只做到跑通留数据，未接入线上站点。

**规模**：Python 流水线约 3,800 行 + Worker 580 行 + 前端约 2,600 行 + 文档约 1,430 行。

---

# ② 结果样例：一次完整运行

## 2.1 漏斗回放（`RUN-20260907-1227`，线上首页可直接查看）

```
采集原始条目                   916
  ├─ URL 规范化后重复          -357   此前已采集，零 LLM 成本
  ├─ 超出采集窗口              -9
  ├─ 关键词粗筛淘汰            -431   与观察范围无关，未进模型
  └─ 转载近重复（SimHash）     -25
入库待处理                      94   通过率 10%
  └─ LLM 预筛                  75 保留 / 19 淘汰
       └─ 结构化抽取           40 条事件草稿
            └─ 归并            37 个事件

本轮成本：LLM 56 次调用 ¥0.3418 ｜ Exa $0.294
配置指纹：350a4bde0383（同一指纹 + 同一输入必然复现同一结果）
```

**每一格都可下钻**：首页漏斗的每个格子点进去，是该轮的拒绝台账明细——
被判为重复、旧闻、无关的条目分别是哪些、在哪一级、因为什么被拦下。
当前台账累计 5,796 条，其中 3,698 条是**在调用模型之前**被零成本关键词粗筛拦下的。

## 2.2 原始信息明细 → 分类核验 → 洞察输出

以事件 **`EV-d0ef0366`**（线上可查：<https://ai.kvkvkvv.site/events/EV-d0ef0366>）为例，展示完整链路。

**第一步：原始条目明细**（5 条，全部可点开原文）

| 一手性 | 信源 | 通道 | 发布 | 标题 |
|---|---|---|---|---|
| C | 新浪科技 | exa | 2026-08-26 | AI客服"已读乱回"谁担责？新国标9月1日强制转人工…… |
| C | 新浪科技 | exa | 2026-08-31 | AI客服"转人工"难？"新国标"来了 |
| C | 新浪科技 | exa | 2026-09-01 | AI客服"已读乱回"、转人工难于登天……今天起，新国标解决痛点！ |
| C | 网经社 | exa | 2026-09-01 | 上海汉盛律师事务所李旻：AI客服新国标衔接《消法》 |
| C | 新浪科技 | exa | 2026-09-02 | AI 与人工客服流转规则有"标"可依 |

每条都存有 URL、采集载体、原始出处、发布时间与时间口径、采集时间、通道，
在 `data/items.jsonl` 里一行一条，GitHub 网页上可直接阅读。

**第二步：分类核验过程**（纯规则，可回归）

```
5 条原始条目
  → 按 org 去重：新浪科技 4 条算 1 个组织，网经社 1 条算 1 个
  → 独立组织数 = 2
  → 无 A 类当事方直述，独立组织数 ≥ 2
  → 置信度判定：多源已验证
  → 落地阶段：已上线（依据第三方报道）
  → 状态：新增（event_date 2026-09-01 落在 30 天观察窗口内）
```

> **按 org 去重而不是按信源去重**，是因为同一家机构的多个渠道发同一件事，
> 不构成独立印证。这条规则在实测中拦下过若干「虚假多源」。

**第三步：洞察输出**（三类断言分开，结构约束在程序层强制）

*事实*（必须挂信源，不得来自 D/E 类）：

- 2026 年 9 月 1 日起，国家标准《顾客联络服务 人工与智能客户服务协同要求》正式实施
- 标准明确涉及价格、折扣、退款、赔偿、合同变更等事项，AI 可以解释规则、收集材料、生成工单，最终确认应当由人工客服或规范化流程完成
- 中国消费者协会 2026 年上半年投诉分析指出，AI 客服投诉集中在转人工机制不畅、不实承诺、生成内容失准等方面

*建议*（**必须没有信源**，由系统基于全局事实生成）：

| 适用 | 内容 | 需内部数据验证 |
|---|---|---|
| 运营 | 对照新国标全面检查客服系统中 AI 与人工的协同流程，确保价格、退款等关键环节由人工确认，优化转人工机制 | 是 |
| 技术 | 9 月 1 日前完成客服系统合规自查：所有客服菜单显性设置"转人工"入口，资金交易与账户安全场景强制自动转人工 | 是 |

> 「建议不能有信源」是整套设计里最关键的一条约束：
> **能追溯到某篇文章的就不是建议，是别人的观点**——那属于「推断」，要写明是谁说的。

## 2.3 可追溯、可查询的证据

**可追溯**

- 每条事实挂着 `source_item_id`，点得到原文链接
- 每条原始条目记着「采集载体 / 原始出处 / 归因后一手性」三个字段——
  36 氪转载晚点的独家，载体记 36 氪，出处记晚点，核验只认归因后的类别
- 每条被淘汰的信息进拒绝台账，带拦截层与原因码
- 每次人工复核留痕进 `data/reviews.jsonl`，含裁决理由
- 真相源是 JSONL，**在 GitHub 网页上直接可读**，不需要跑任何程序

**可查询**

| 方式 | 入口 |
|---|---|
| 网页筛选 | 事件库支持按主体 / 领域 / 状态 / 置信度 / 落地阶段组合筛选 |
| REST API | `GET /api/v1/events?company=meituan&confidence=官方一手` → 16 条（全库「官方一手」恰好全部来自美团技术团队这个 A 类信源） |
| 语义检索 | `GET /api/v1/search?q=...`，词面 + 向量混合（查「用AI帮商家做投放素材」能命中零字面重合的「AI模特与种草文案生成」） |
| 自然语言 | 定制简报页输入问题，现场检索知识库 + 互联网生成报告 |
| SQL | `python3 -m pipeline.run --rebuild` 后直接查 `intel.db` |

## 2.4 当前知识库

<!-- live:counts -->
| | |
|---|---|
| 事件 | 167 |
| 原始条目 | 699 |
| 断言（事实 1250 / 推断 242 / 建议 331） | 1823 |
| 事件关系边 | 192 |
| 拒绝台账 | 6,291 |
| 登记信源（其中 23 个有实际产出） | 36 |
| 周报 | 19 期 |
| 复核记录 | 74 |
| 运行轮次 | 13 |

事件日期跨度 2023-05-24 ~ 2026-09-07。
<!-- /live -->

<!-- live:confidence -->
| 置信度 | 事件数 |
|---|---|
| 官方一手 | 16 |
| 多源已验证 | 20 |
| 深度单源 | 59 |
| 单源待确认 | 72 |

独立组织数为 1 的有 131 个，占 78%。
<!-- /live -->

<!-- live:stage -->
| 落地阶段 | 事件数 |
|---|---|
| 概念宣传 | 16 |
| 试点探索 | 24 |
| 已上线 | 92 |
| 规模化应用 | 28 |
| 未判定 | 7 |
<!-- /live -->

---

# ③ 运行说明

## 3.1 观察范围

<!-- live:scope -->
**观察主体（9）**：阿里巴巴 · 抖音电商 · 快手电商 · 小红书电商 · 京东 · 美团 · 微信小店 · AI 模型与工具厂商 · 行业·监管

**观察领域（6）**：商品与零售运营 · 营销与内容生成 · 商家工具与服务 · 数据与商业分析 · 客服与履约 · AI 能力供给
<!-- /live -->

**我方视角**：本站服务于京东零售，每个事件都要求给出对我方的启示，并区分
① 竞对能力构成的压力 ② 可直接借鉴的做法 ③ 上游能力变化带来的新可能。
京东自身的动态同样观察——追踪自家公开足迹是竞对情报的常规做法。

**信源体系**：按**一手性**分层（分层轴是「这条信息离事件现场有多近」，不是发布主体的名气）：

| 类 | 定义 | 核验路径 |
|---|---|---|
| A 当事方直述 | 官方公告 / 财报 / 招聘 JD | 不需多源交叉，但**落地阶段要打折** |
| B 原创深度报道 | 有独立采访、内部信源 | 单源即可进「深度单源」 |
| C 常规媒体报道 | 基于公开信息的日常报道 | **必须多源交叉** |
| D 聚合转载 | 无增量信息的搬运 | 不计独立信源，只作归并证据 |
| E 观点评论 | 个人判断与分析 | **禁止抽取事实** |

## 3.2 时间口径

**三级时间口径**，来源不同不能混用：

| 口径 | 含义 | 参与窗口硬判定 |
|---|---|---|
| `exact` | 原文明确给出的发布时间 | 是 |
| `parsed` | 从标题或正文解析出的日期 | 是 |
| `inferred` | 只能推断（"本周""近日"） | **否** |

**两个窗口**：采集窗口 250 天（往回捞多久，用于建立历史背景），观察窗口 30 天（决定"新增 / 延续 / 静默 / 旧闻"）。

**事件的时间锚点是 `event_date`（事情发生的时间），不是系统发现的时间。**
这两者混用会让回填的历史事件全部被标成"新增"——这个 bug 出现过，一次拦下 87 条追溯性事件。

周报的周期口径同理，是**事件发生周**而不是"本轮采集到"，因此历史周报可以回填。

## 3.3 更新频率

两级节奏：**事件库每天两轮，周报每周一份。**

| | 频率 | 说明 |
|---|---|---|
| 事件采集与核验（S0–S6） | 每天两轮，北京时间 **08:30 / 20:30** | cron 为 UTC 00:30 / 12:30；晚场只跑免费通道以控制检索额度 |
| 周报合成（S7） | **每周一份**，周一早上第一轮产出上周报告 | 当周后续 13 轮全部跳过，零模型成本 |

**周报为什么不按星期判断。** S7 每轮都检查「上一个完整自然周」的周报是否已存在，不存在才生成。效果与「周一 08:30 生成」完全一致，但多了自愈：GitHub 的定时任务是尽力而为、会延迟甚至整条丢弃（本仓库实测到 12:30 那轮被推迟到 15:36，另有一轮完全没触发），模型调用也可能失败——按星期判断一旦错过就永久缺一期，按存在性判断则下一轮自动补上。

改动前 S7 每轮都重算并覆盖，同一周的周报一天内被生成 4 次，既白花模型钱，内容还每轮都在变。

**补位 cron。** 同样因为 schedule 不可靠——本仓库实测延迟是常态而非偶发，2026-09-07 六条 cron 无一准点，00:30 那条 05:02 才跑（迟 4h32m），12:30 那条 17:49 才跑（迟 5h19m）——每个正式档挂满 7 小时的补位 cron，哪条先落地哪条跑，其余的十几秒内被判掉。

**补位轮的判据同样是存在性，不是时间差。** 早先用的是「距上次运行不足 6 小时就跳过」，这在延迟面前会自我抵消：迟到越多，补位轮离上一轮越近，越容易被自己的闸门拦掉。09-07 早场差 28 分钟就整轮消失（07:29 那轮距上次 6.47 小时，只比闸门多 0.47），而且不会有任何告警。现改为「本档今天服务过没有」，与周报的判法一致，迟到多久都不影响判断。本地手动跑的轮次也计入——「每天两轮」说的是数据被刷新两次，不是 CI 被触发两次。

**迟到不会静默。** 跑批迟于正式档一小时以上时，Actions 里挂 warning 写明迟了多久，运行摘要也带上档位与判定理由，避免「作业全绿」被读成「数据按点更新」。

**同步延迟。** 数据从跑批到线上最慢约 15 分钟（GitHub raw 的 CDN 缓存 + Worker 边缘缓存 600 秒）。页面底部标着快照时间。

## 3.4 输入与输出

**输入**

| 类型 | 内容 |
|---|---|
| 配置 | `config/ecommerce.json`（主体、领域、检索矩阵、关键词、窗口）+ `config/sources.ecommerce.json`（信源注册表） |
| 凭据 | `KB_LLM_API_KEY`（DeepSeek，必需）、`EXA_API_KEY`（检索，必需）、`JINA_API_KEY`（列表页抓取与向量，可选） |
| 数据 | 六通道采集的公开信息 |

**输出**

| 产物 | 位置 |
|---|---|
| 结构化事件库 | `data/events.jsonl`、`data/claims.jsonl` |
| 原始条目明细 | `data/items.jsonl` |
| 拒绝台账 | `data/rejects.jsonl` |
| 运行档案 | `data/runs.jsonl`（漏斗各级计数、成本、配置指纹） |
| 周报 | `data/reports.jsonl` |
| 复核留痕 | `data/reviews.jsonl` |
| 只读 API | `/api/v1/*`，错误统一 Problem JSON |
| 网页 | 运行回放 / 事件库 / 周报 / 定制简报 / 信源 / 文档 |

**成本**：一轮全通道约 Exa $0.26 + LLM ¥0.18，月成本约 $8 + ¥11。

## 3.5 人工复核节点

系统在三处主动把不确定性交给人：

| 节点 | 触发条件 | 处理方式 |
|---|---|---|
| **单源待确认** | 只有一个独立组织支撑 | 进复核队列，人工判断采信 / 观察 / 驳回 |
| **疑似重复** | 归并阶段模型明确表示"拿不准" | 标 `suspect_duplicate`，人工裁定合并或拆分 |
| **信源分级调整** | 实际产出与登记等级不符 | 人工调整 `source_class` 与 `reliability` |

复核走本地 CLI（`python3 pipeline/review.py queue`），不上线——站点是纯只读的。
每次裁决留痕进 `data/reviews.jsonl`，含理由；批量裁决记一条记录而不是逐条重复。

**设计要点**：归并阶段刻意不做二选一。早期版本要求模型在"合并 / 不合并"之间选，
并强调"宁可漏掉"，结果模型交回了空数组——**强迫在不确定的地方做二元决策，
得到的不是保守，是弃权**。现在输出增加 `suspects` 一档，不确定性被显式表达出来。

<!-- live:review -->
复核态：持续观察 73 · 已确认 29 · 无需复核 63 · **待复核 2**
<!-- /live -->

## 3.6 不足与改进方向

按影响程度排序。每条都写清楚**现状是什么、为什么会这样、打算怎么改**。

### ① 覆盖偏差：Exa 贡献了近七成条目

**现状**：687 条原始条目里 exa 占 457 条。检索服务的召回偏好会直接传导为覆盖偏差——
**没被检索到的事情，这个系统不知道自己不知道**。

**成因**：中文电商垂类的稳定订阅源供给远不如预期。实测 RSSHub 的 5 个路由族里 4 个已失效，
多数官网是 CSR 页面直抓不到正文。这不是设计选择，是被现实逼出来的。

**改进**：
1. 引入第二家检索服务（如 Tavily / Brave Search）做交叉召回，用两家的差集衡量单一来源的盲区有多大——**先把偏差量出来**，再谈缩小
2. 为官网 CSR 页面接入无头浏览器渲染（Playwright），把「抓不到」的官方渠道转成可采
3. 建立召回率抽检：人工挑一批已知事件，回测系统是否采到，得出可量化的漏采率

### ② A 类信源仍然稀薄，导致阶段打折规则几乎不触发

**现状**：本轮接入美团技术团队 RSS 后，A 类条目从全库 2 条涨到 20 条，
「官方一手」这个置信度档位从 0 个事件涨到 16 个。但**「官方口径被第三方下修」这个
具体情形至今只出现过 1 次**——因为官方与第三方**同时报道同一件事**的重叠仍然稀少。

**成因**：A 类要发挥作用需要两个条件同时满足：官方发了、第三方也报了。目前多数 A 类事件
是官方单方面披露，没有对照面。

**改进**：
1. 按美团这条路径继续挖：各平台的技术博客、开发者社区、招聘 JD 都是可采的 A 类，
   且实测 RSS 存活率明显高于新闻类官网
2. 把财报电话会、开发者大会实录纳入采集——这类内容天然会被第三方同步报道，能制造对照面
3. 对「仅官方口径，未经第三方验证」的事件（当前 16 个）加显式提示，
   让读者知道这条的阶段判断没有外部制衡

### ③ 单源事件占比仍然偏高

**现状**：独立组织数为 1 的事件占多数（实时占比见 2.4 的置信度分布）。系统不隐藏，全部标注并进复核队列，
但这确实意味着相当一部分结论没经过交叉验证。

**成因**：观察窗口内很多动作本身只有一家媒体报道，尤其是二线平台的中小动作。

**改进**：
1. 延长「等待第二信源」的观察期并自动复查——现在转「持续观察」后是靠人工回看，
   应该做成规则：两周内出现第二个独立组织就自动升档，没有就自动降级
2. 归并阶段引入跨轮次匹配——目前 S4 只看本轮候选，历史事件靠 S4b 的向量召回补，
   可以把新条目直接与全库事件做向量匹配，提高「同一事件的后续报道」被正确挂上的比例

### ④ 洞察深度止步于「事件级」

**现状**：周报能跨事件看格局，但事件之间的关系边目前以规则生成的
`same_actor_track`（同主体时间相邻）为主，模型提议的语义关系（follows / causes /
contradicts）数量少。也就是说，**系统能告诉你「谁做了什么」，还不太能告诉你「因为什么」**。

**改进**：
1. 用向量召回补关系边——现在向量只用在归并复核，同样可以召回「可能存在因果或跟随关系」
   的事件对交给模型判定
2. 引入「主题线」概念：把同一条演进脉络上的事件串成时间线（如「AI 导购从概念到规模化」），
   而不只是两两连边
3. 周报增加「上期判断的兑现率」统计——目前有延续性检查，但没有把它量化成可回溯的准确率

### ⑤ 未接入内部数据，建议无法闭环

**现状**：所有涉及我方效果的建议都标「需内部数据验证」，因为公开信息看不到京东零售的
成本结构与存量能力。建议因此停在「值得评估」这一层，给不出「该不该做」。

**改进**：这条的解法不在技术侧——需要与业务方约定一小组可对接的内部指标
（如某类目的 AI 导购渗透率、客服转人工率），把「需内部数据验证」的建议从标注变成可验证的假设。
在拿到数据之前，保持标注比假装能下结论更负责。

### ⑥ 工程侧的已知欠账

| 项 | 现状 | 改进 |
|---|---|---|
| 无自动化测试 | 全靠真跑一轮观察数据是否合理 | 至少给 S1 去重与 S5 核验这两个纯规则阶段补回归测试——它们输入确定则输出确定，最适合断言 |
| 第二数据集未接入站点 | 汽车数据集只跑通留了数据 | 前端支持数据集切换，或至少给它一个只读的对照页 |
| 前端离线兜底不完整 | 只覆盖首页与列表，详情页仍需联网 | 把详情页的最小集也嵌入，或改用 Service Worker 缓存 |
| 成本无预算闸 | 靠人工看余额 | 在流水线里加额度检查，接近阈值时自动降级为免费通道 |

---

## 3.7 安全与合规边界

- 只采集**公开可访问**信息，遵守 robots.txt，**不绕过登录墙与付费墙**
- 付费墙内容只取标题与导语，标记 `partial_content`
- 正文只保留**截断摘录 + 原文链接**，不存储也不再分发第三方全文
- AIHOT 作为发现渠道接入：落库一律用 `links.original` 指向的原文，
  信源按原文发布方归属而非记为 AIHOT
- 输出仅供内部研究参考，**不构成投资建议**
- 涉及我方业务的借鉴建议均标注「需内部数据验证」
- 密钥全部走环境变量与 GitHub / Cloudflare Secrets，仓库内无明文凭据；
  CI 提交前有暂存区密钥扫描闸

---

> 设计依据、踩坑记录、完整数据模型与 API 契约见站内文档：<https://ai.kvkvkvv.site/docs>
